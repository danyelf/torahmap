node_modules
dist
*.local
# The Python toolchain for the search index generator. Created by the setup in
# public/data/search/README.md, and about 300MB once text-fabric is in it.
.venv
.playwright-mcp/
.worktrees/
.agents
.csb
coverage
# No trailing slash: in a worktree this is usually a symlink to the copy in the
# main checkout, rather than 650MB of CSVs downloaded a second time.
data/overlays/commentary/sefaria-links
data/overlays/commentary/sefaria-index.json
.DS_Store
.claude/worktrees
__pycache__/

# Talmud verification cache — regenerable via scripts/talmud/verify-coverage.ts
data-transient/

# Visual brainstorming companion scratch
.superpowers/

# Local state from `wrangler dev`
.wrangler

# Layout test output (npm run test:layout)
test-results/
layout-report/
playwright-report/
