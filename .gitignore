node_modules
dist
*.local
.playwright-mcp/
.worktrees/
.agents
.csb
coverage
# No trailing slash: in a worktree this is usually a symlink to the copy in the
# main checkout, rather than 650MB of CSVs downloaded a second time.
data/sefaria-links
.DS_Store
.claude/worktrees
__pycache__/

# Talmud verification cache — regenerable via scripts/talmud/verify-coverage.ts
data-transient/
